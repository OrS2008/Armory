# Frontend build-time configuration
VITE_API_BASE=/api/v1

# Local Pages Functions secrets belong in .dev.vars (never commit it):
#   BOOTSTRAP_ADMIN_EMAIL=admin@example.mil
#   BOOTSTRAP_ADMIN_PASSWORD=<a long random password>
