name = "shabatzak"
compatibility_date = "2026-08-01"
pages_build_output_dir = "./dist"

# Cloudflare Pages reads this file and it takes precedence over the dashboard's
# binding settings, so the real database id has to live here.
[[d1_databases]]
binding = "DB"
database_name = "shabatzak"
database_id = "cdd2fb8a-d82e-4b50-bf1d-02a5f103aef6"
migrations_dir = "migrations"
